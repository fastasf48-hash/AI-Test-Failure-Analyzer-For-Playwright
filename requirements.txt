# --- Test automation ---------------------------------------------------------
playwright==1.61.0
pytest==9.1.1
pytest-playwright==0.8.0
pytest-html==4.2.0
allure-pytest==2.16.0

# --- Dashboard & data -----------------------------------------------------------
streamlit==1.59.2
pandas==3.0.3
plotly==6.9.0

# --- Persistence ----------------------------------------------------------------
SQLAlchemy==2.0.51

# --- LLM providers (pluggable; both installed, only the configured one is used) --
openai==2.46.0
anthropic==0.117.0
pydantic==2.13.4

# --- Config & env -----------------------------------------------------------------
python-dotenv==1.2.2

# --- Lint / format (dev only) ------------------------------------------------------
ruff==0.15.22
black==26.5.1
